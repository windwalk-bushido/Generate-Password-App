import { createStore } from 'vuex'


export default createStore({
	state() {
		return {
			//menu_state: false,
		}
	},

	mutations: {
		//SetMenuState(state, param_boolean) { state.menu_state = param_boolean; },
	},

	getters: {
		//GetMenuState(state) { return state.menu_state },
	}
})
